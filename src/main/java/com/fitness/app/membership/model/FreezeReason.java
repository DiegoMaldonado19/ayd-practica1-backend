package com.fitness.app.membership.model;

/** Why the membership was paused: "por ejemplo, por viaje o lesión" plus the frequent cases. */
public enum FreezeReason
{
    TRAVEL,
    INJURY,
    ILLNESS,
    WORK,
    OTHER
}
