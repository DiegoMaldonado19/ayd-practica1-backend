package com.fitness.app.membership.model;

/**
 * What the other modules ask about with MembershipService.hasBenefit. The statement
 * fixes which benefits exist, so this is an enum and not a benefit table.
 */
public enum PlanBenefit
{
    GROUP_CLASSES,
    PERSONAL_TRAINER
}
