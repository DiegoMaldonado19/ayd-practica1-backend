package com.fitness.app.membership.model;

/** Why the member ended the relationship. Optional, and useful for the administrative reports. */
public enum CancellationReason
{
    COST,
    RELOCATION,
    DISSATISFACTION,
    INJURY,
    OTHER
}
