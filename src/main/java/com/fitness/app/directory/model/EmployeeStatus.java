package com.fitness.app.directory.model;

/** Mirrors ck_employee_status. TERMINATED is the resignation that empties a trainer's caseload. */
public enum EmployeeStatus
{
    ACTIVE,
    SUSPENDED,
    TERMINATED
}
